# 新手从零开玩 · 三步上手

> 给**电脑上还没装过任何 AI 工具**的朋友。全程不需要懂编程，只需**粘贴几条命令**；每一步卡住都可以直接把报错或界面内容发给 AI 助手问"怎么弄"。
> 界面文案可能随版本更新略有变化，找不到按钮时搜索关键词即可。

---

## 整体流程（三步）

1. **装 Kimi Code CLI**（一条命令安装，手机号登录，自带免费额度）
2. **下载本项目**（网页点下载 ZIP，解压）
3. **打开项目文件夹，说「开始玩吧」**

全程约 15 分钟。之后想省钱/更好玩，再看「进阶：换 DeepSeek API」。

---

## 第一步：安装 Kimi Code CLI

> Kimi Code 是**终端里运行的 AI 助手**（命令行版本），本项目按它的习惯编写，兼容最好。自带免费模型额度，先不花一分钱就能玩。

**0）装 Git for Windows（Windows 前置，官方要求）**

1. 打开 https://git-scm.com 下载 Windows 版，双击安装，一路"下一步"（全默认）。

**1）一条命令装 Kimi Code CLI**

1. 按键盘 `Win + R`，输入 `powershell`，回车，打开蓝色窗口；
2. 粘贴下面这行命令，回车（自动下载安装，**不需要先装 Node.js**）：

```
irm https://code.kimi.com/kimi-code/install.ps1 | iex
```

3. 看到安装完成的提示后，**关掉窗口重开一个** PowerShell。

**2）登录**

1. 在新窗口里输入 `kimi login` 回车；
2. 浏览器会自动打开登录页，用**手机号**登录（收个验证码），登录后回到窗口即完成。

> 安装命令以官方文档为准：https://www.kimi.com/help/kimi-code/cli-getting-started （备选：npm 全局安装，见官方文档）。

---

## 第二步：下载本项目

二选一（内容相同，哪个快用哪个）：

- **GitHub**：打开 https://github.com/a1206122381-cmyk/-s-AI-RPG → 点绿色 **Code** 按钮 → **Download ZIP** → 下载后**解压**；
- **Gitee**：打开 https://gitee.com/hkfg1206/s-AI-RPG → 点 **克隆/下载** → **下载 ZIP** → 解压。

解压后得到一个 `s-AI-RPG` 文件夹，**记住它的位置**（比如放到桌面）。

---

## 第三步：开始玩

1. 打开 PowerShell（或"Windows 终端"）；
2. 进入项目文件夹：输入 `cd ` 后空格，把解压出的文件夹**拖进窗口**（会自动补全路径），回车；例如：

```
cd C:\Users\你的用户名\Desktop\s-AI-RPG
```

3. 输入 `kimi` 回车，进入对话界面；
4. 输入「**开始玩吧**」回车；
5. 它可能会自动检查/补装环境（按提示操作，或直接同意）；缺东西时它会给你下载链接，装完回来再说一次「开始玩吧」；
6. 一切就绪后它会开始四阶段建世界（世界 → 政治地缘 → 人物与势力 → 正式游玩），期间会和你讨论设定——**你想怎么玩就怎么说**，它听你的。

> 退出对话：按 `Ctrl + C`（以界面提示为准）。
> 存档：玩的过程中随时说「存档」，它会自动保存并给你一个存档编号。
> 换电脑/换对话续玩：打开项目文件夹运行 `kimi`，说「开始玩吧」会自动恢复；更稳的方式是把 `RESUME.md` 里的恢复提示词整段复制粘贴给它。

---

## 进阶：换 DeepSeek API（免费额度用完后推荐）

Kimi Code 自带的免费额度用完（或想用更适合跑团的模型）后，推荐换 **DeepSeek 官方 API**：便宜、中文叙事强、上下文长。

### 1. 注册并充值

1. 打开 https://platform.deepseek.com ，手机号注册登录；
2. 左侧「**充值**」：支付宝/微信充值（10 元起，够玩很久——一场几小时的游戏通常只花几块钱，具体以官网定价页为准：https://api-docs.deepseek.com/zh-cn/quick_start/pricing/ ）。

### 2. 创建 API Key

1. 左侧「**API Keys**」→「创建 API Key」；
2. 起个名字（随便），点创建；
3. **立即复制**那串 `sk-` 开头的密钥（只显示这一次，丢了就重新创建）。

### 3. 配置到 Kimi Code（推荐：让它自己配）

在 Kimi Code 对话里直接说：

```
请帮我把 DeepSeek API 配置进来：
Base URL: https://api.deepseek.com
API Key: sk-（粘贴你的密钥）
模型名: deepseek-chat
```

它会自己改配置文件。手动方式：对话里输入 `/models`（模型配置界面）添加 OpenAI 兼容提供商，填入上面的 Base URL / Key / 模型名（界面文案以实际为准）。

### 4. 用起来

配置好后切到 `deepseek-chat` 模型，继续说「开始玩吧」。

> 想先免费试：硅基流动（siliconflow.cn）注册送免费额度，同样把 Base URL + Key + 模型名交给 Kimi Code 配置，模型可选 DeepSeek/Qwen/GLM 系列。

---

## 常见问题

**Q：免费额度用完了怎么办？**
换 DeepSeek API（见上），或继续用 Kimi Code 的免费/付费方案。

**Q：它说没有权限运行命令？**
按提示允许（输入 `y` / 回车确认）。本项目需要读写文件 + 运行命令才能存档。

**Q：怎么"打开项目文件夹"？**
两种方式：① `cd` 后把文件夹拖进终端窗口自动补路径；② 在资源管理器里打开文件夹，地址栏输入 `powershell` 回车，直接在这个位置打开终端，再输入 `kimi`。

**Q：换个电脑怎么接着玩？**
重装 Kimi Code CLI → 重新下载本项目（zip 或最新版）→ 打开文件夹 → 说「开始玩吧」。存档存在云端仓库里，会自动同步下来。

**Q：玩到一半换了个对话，怎么续？**
打开项目文件夹后运行 `kimi`，把 `RESUME.md` 里的恢复提示词整段复制粘贴给它，它会自动接上。

**Q：它生成的内容乱了/和之前对不上？**
说「存档」，然后新开对话贴 RESUME 提示词恢复，一般就能接上。

---

## 附录 A：DSH（DeepSeek Harness）安装教程（进阶，DeepSeek 适配更好）

> DSH 对 DeepSeek 模型的适配（思考模式等）比 Kimi Code 更到位，安装同样走命令行。愿意动手的朋友走这条路线，效果更佳。

### 1. 装 Node.js

1. 打开 https://nodejs.org ，下载 **LTS 版** Windows 安装包；
2. 双击安装，一路"下一步"（全默认即可）；
3. 装完按 `Win + R`，输入 `cmd` 回车，在黑色窗口里输入 `node --version` 回车——出现版本号（如 v24.x）即成功。

### 2. 装 DSH

在同一个黑色窗口里输入并回车：

```
npm install -g @deepseek-ai/dsh
```

装完后输入 `dsh --version` 出现版本号即成功。

### 3. 启动并配置 DeepSeek API

1. 输入 `dsh web` 回车，浏览器会自动打开（或手动访问 http://127.0.0.1:3080 ）；
2. 先按上面「进阶」第 1-2 步注册 DeepSeek 并创建 API Key；
3. 在 DSH 界面：**设置 → 模型** → 选 **deepseek-official**（DeepSeek 官方）→ 填入 API Key → 保存；
4. 新建对话 → 把**工作目录**设为解压出的 `s-AI-RPG` 文件夹 → 说「开始玩吧」。

> 其余体验（存档/续玩/环境自举）与 Kimi Code 路线完全一致；本项目对 DSH 的适配说明见 `DSH_ADAPTER.md`。

---

*本项目为个人创意项目，AI 辅助运行；世界、人物与剧情纯属虚构。玩得开心！*
